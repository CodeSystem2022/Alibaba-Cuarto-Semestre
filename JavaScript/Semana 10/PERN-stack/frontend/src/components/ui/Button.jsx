


function Button({ children }) {
  return (
    <button className="relative inline-flex items-center gap-1.5 rounded-md px-3 py-1.5 text-sm font-semibold
    text-while shadow-sm hover:bg-blue-400 focus-visible:outline focus-visible">{Children}</button>
  )
}

export default Button